name: seeko_example
description: Production-quality capability catalog for the Seeko package.
publish_to: none
version: 1.0.2

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.32.0"

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  seeko:
    path: ../

dev_dependencies:
  flutter_cockpit: ^4.0.26
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

melos:
  useRootAsPackage: true
